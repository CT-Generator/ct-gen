## 1. Middleware: narrow the cookie-write surface and reorder resolution

- [x] 1.1 In `web/middleware.ts`, change the locale resolution order to: cookie → URL prefix → Accept-Language → `en`. The URL prefix is still parsed (and used to determine path rewriting), but only consulted for *chrome* locale when no cookie is present.
- [x] 1.2 Replace the line `if (locale !== cookieLocale) setLangCookie(res, locale);` with a guard that only writes the cookie when **no** `cgen_lang` cookie was sent on the incoming request (the first-visit imprint case).
- [x] 1.3 Keep the first-visit Accept-Language redirect block intact (it already writes the cookie via `setLangCookie(res, acceptLocale)`); confirm permalink paths (`/g/`, `/story/`) still skip this redirect via `isPermalink`.
- [x] 1.4 Keep the path-rewrite branch (`if (explicitPrefix) { res = NextResponse.rewrite(...) }`) — chrome locale and path rewriting now decouple, but the rewrite itself still happens when a `/de/` or `/nl/` prefix is in the URL.
- [x] 1.5 Verify the `parseLocale` and `parseAcceptLanguage` helpers are unchanged; remove any now-unused branches.

## 2. Permalink pages: drop the row-locale redirect, separate chrome from content

- [x] 2.1 In `web/app/g/[id]/page.tsx`, delete the `if (visitorLocale !== rowLocale) redirect(...)` block (around lines 141–144).
- [x] 2.2 In the same file, rename the `rowLocale` use sites for *chrome* concerns (masthead doesn't take props here, but anything that builds a navigation link to `/recipe` etc. for the masthead should use the chrome locale, not `rowLocale`). The recipe/CTA link at line 310 (`href={localizedHref("/recipe", rowLocale)}`) is a soft case — it points to the recipe page; align this with the visitor's chrome locale so the destination matches every other "Recipe" link in the chrome. Read the chrome locale from `readLocale()` and use it for `localizedHref` calls that target chrome destinations; keep `rowLocale` only for the page's *content* dictionary lookups and the `<html lang>` override. (Done: added `chromeLocale = await readLocale()` and used it for the recipe link list and the "build another" home link; `rowLocale` still drives dictionaries, content, OG metadata, and share permalink. The `permalink` variable used by ShareButtons is left as the un-prefixed `/g/<id>` URL — pre-existing behavior, not in scope.)
- [x] 2.3 In `web/app/story/[uuid]/page.tsx`, delete the analogous `if (visitorLocale !== eventLocale) redirect(...)` block (around lines 38–41). Keep `eventLocale` for content (paragraphs, H1, dictionary lookups); use the chrome locale (`visitorLocale` from the `readLocale()` call already on line 27) for any internal navigation link targets in the page body (e.g., the home-link at line 75 should use chrome locale, not `locale = eventLocale`). (Done: renamed `visitorLocale` to `chromeLocale`; "pick different" home link now points to chrome-locale home. ConspiratorsPicker still receives `eventLocale` because its CTA starts a build in that locale.)
- [x] 2.4 In `web/app/build/[id]/page.tsx`, confirm the wizard content already follows `rowLocale` (it does — line 43). Add a `chromeLocale = await readLocale()` and thread it to anything that needs to render in the visitor's UI language (masthead is already self-resolving via `readLocale()`; nothing else to thread here unless a wizard nav link surfaces a chrome-destination href). (Done: verified — no thread-through needed. Masthead self-resolves; the wizard's "view result" navigation correctly uses rowLocale because it's a permalink target. Mid-build chrome flip via the toggle is acceptable per design Decision 5.)
- [x] 2.5 Audit every other page (`recipe`, `teach`, `about`, `imprint`, `privacy`, home, not-found) — these are not driven by a row, so `readLocale()` returns the chrome locale and the chrome locale equals the content locale. No changes needed; capture the audit result in a tasks.md comment if anything looks off. (Done: audited via the earlier href grep — all internal `localizedHref` calls on these pages already source from `readLocale()`/chrome locale, since these pages are not row-driven. No changes needed.)

## 3. `<html lang>`: align with content language on permalink pages

- [x] 3.1 In `web/app/layout.tsx`, replace the hard-coded `<html lang={locale}>` (where `locale = await readLocale()` resolves to chrome locale) with a value that can be overridden per route. Simplest mechanism: introduce a `x-content-locale` request header — set by the page itself via a small helper invoked at render — that the layout reads (falling back to `x-locale` when absent). (Done with a different mechanism: instead of a page-set header, the layout itself reads `x-pathname` and pattern-matches `/g/<id>`, `/story/<uuid>`, `/build/<id>` to resolve the row/seed locale directly. Avoids the page→layout context-passing problem entirely. See `lib/i18n/content-locale.ts`.)
- [x] 3.2 Add a helper `setContentLocale(locale: Locale)` in `web/lib/i18n/index.ts` that no-ops on most pages but, when called from `/g/[id]`, `/story/[uuid]`, or `/build/[id]`, attaches the row/event locale to a per-request store the layout can read. (If headers can't be mutated from a page, use a React context provider in the route's layout instead.) (Superseded: implemented `resolveContentLocaleFromPath(path)` instead. Uses `react.cache()` on `getGenerationRowLocale(shortId)` so the row-locale lookup dedupes across layout + page when the page also fetches the row. Seed lookup for `/story/<uuid>` is sync. Net cost: one extra small DB query per permalink page for the locale column only.)
- [x] 3.3 Update `/g/[id]/page.tsx`, `/story/[uuid]/page.tsx`, and `/build/[id]/page.tsx` to call `setContentLocale(rowLocale)` (or the chosen mechanism) before they render. Verify that the rendered HTML for an English visitor on `/de/g/<id>` (German row) contains `<html lang="de">`. (Implicit: with the path-detection mechanism, pages don't need to call anything — the layout reads the path and looks up the row itself. Verification belongs to manual task 6.4.)
- [x] 3.4 Verify all non-permalink pages still emit `<html lang={chromeLocale}>` (no regression). (Verified by inspection: `resolveContentLocaleFromPath` returns `null` for any path other than `/g/<id>`, `/story/<uuid>`, `/build/<id>`, and the layout falls back to `chromeLocale` when it gets null.)

## 4. Masthead and toggle: confirm chrome-locale sovereignty

- [x] 4.1 In `web/components/zine/topbar.tsx`, confirm `readLocale()` continues to return the cookie-resolved chrome locale (which it does, since `x-locale` now carries chrome locale per Decision 3 in design.md). No code change expected; verify by reading the file. (Verified: topbar.tsx unchanged, uses `readLocale()` → chrome locale.)
- [x] 4.2 Confirm `topbar-client.tsx`'s `writeLocaleCookie` is still the only place outside middleware that writes `cgen_lang`. No code change expected; verify by grepping `web/` for `cgen_lang` writes. (Verified via grep — only `middleware.ts` (server-side, first-visit imprint only) and `topbar-client.tsx` (client-side, on toggle click) write the cookie.)
- [x] 4.3 Verify the toggle navigation behaviour: clicking EN from `/de/recipe` should navigate to `/recipe` (already correct via `localizedHref(path, "en")` on the topbar's locale-options). (Verified by inspection of topbar.tsx — the locale toggle hrefs use `localizedHref(path, l)` where `path = x-pathname` (un-prefixed) and `l` is each locale option.)

## 5. Visitor analytics: confirm chrome-locale capture

- [x] 5.1 In the page-view capture path (called from `app/layout.tsx` via `captureVisit`), confirm the `locale` value written to `page_views` is sourced from `readLocale()` (the chrome locale) and not from the URL prefix. No schema change is expected; this is a verification task. (Verified: `captureVisit` reads `x-locale` header, which middleware sets to the chrome locale.)
- [x] 5.2 Spot-check the `/stats/visitors` view to ensure no per-locale chart assumes "URL prefix = locale". Update tooltips/captions in `web/app/stats/visitors/page.tsx` if any imply the row reflects the URL prefix rather than the user's chrome locale. (Verified: the only locale render is in the "Recent samples" list as a raw token; no tooltip or caption implies URL-prefix semantics.)

## 6. Manual verification

- [ ] 6.1 Start `npm run dev`. Clear cookies. Load `/` with browser language English — expect `/` to render in English, cookie `cgen_lang=en` set.
- [ ] 6.2 Clear cookies. Set browser language to German. Load `/` — expect 302 to `/de/`, cookie `cgen_lang=de` set, German chrome.
- [ ] 6.3 With `cgen_lang=en`, navigate manually to `/de/recipe`. Expect: page renders English chrome + English content (the recipe page itself is single-source-of-truth English), URL bar stays `/de/recipe`, cookie unchanged.
- [ ] 6.4 With `cgen_lang=en`, create a German-locale generation (via /de/ → toggle to DE → through the wizard → produce a row with `locale=de`) — or use an existing DE row in the database. Copy the `/de/g/<id>` URL. Reset cookie to `en` via the toggle. Open the `/de/g/<id>` URL. Expect: German H1 + German body content, English masthead + English nav links + English Recipe/Teach/About targets, `<html lang="de">`, cookie still `en`.
- [ ] 6.5 From that same `/de/g/<id>` page, click "Recipe" in the masthead. Expect: navigation to `/recipe` (NOT `/de/recipe`), recipe page renders in English, cookie still `en`.
- [ ] 6.6 From the `/de/g/<id>` page, click the locale toggle to DE. Expect: cookie set to `de`, page reloads (or re-renders) with German chrome too; subsequent menu clicks stay in `/de/`.
- [ ] 6.7 Repeat 6.4–6.6 for the `nl` locale and a Dutch row.
- [ ] 6.8 With cookie `de`, start a fresh build at `/de/`. Pick an event, run through the wizard. Confirm chrome stays German throughout. Confirm the `/de/build/<id>` page renders German wizard text and German chrome.
- [ ] 6.9 Mid-generation chrome switch: start a build at `/de/` (cookie `de`, row `de`); on a move screen click the locale toggle to EN. Expect: chrome flips to English, URL becomes `/build/<id>`, but the wizard step text stays German (row's locale). Confirm this matches design.md's stated trade-off and feels OK in practice. If it's jarring, file a follow-up issue; do NOT auto-translate the row content as part of this change.

## 7. Documentation and cleanup

- [x] 7.1 Update the comment block at the top of `web/middleware.ts` to reflect the new responsibilities (cookie wins over prefix; cookie is only written on first visit; spec ref points to this change's spec.md).
- [x] 7.2 Remove the now-stale spec reference comments in `/g/[id]/page.tsx` and `/story/[uuid]/page.tsx` that point to "Permalink visit redirects to the row's locale URL when prefixes mismatch" (that requirement is being removed).
- [x] 7.3 Run `npm run typecheck` and `npm run lint` — both must pass. (Both pass clean.)
- [ ] 7.4 When implementation is complete and verified, the change can be archived with `openspec archive sticky-language-selection`. Until then, leave the change in `openspec/changes/`.
